<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Plot | Dashboard</title>
  <link rel="stylesheet" href="/assets/app.css" />
</head>
<body class="bg-gray-100 text-gray-900 font-sans">
  <header class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-xl font-bold">Plot Dashboard</h1>
      <nav>
        <.link navigate={~p"/plots/new"} class="text-blue-600 hover:underline mr-4">New Plot</.link>
        <.link navigate={~p"/users/logout"} class="text-red-600 hover:underline">Logout</.link>
      </nav>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-4 py-10">
    <section class="mb-8">
      <h2 class="text-2xl font-semibold mb-2">Welcome back, <%= @current_user.name %> 👋</h2>
      <p class="text-gray-700">Here’s what’s happening in your plots today.</p>
    </section>

    <section class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div class="bg-white p-6 rounded shadow">
        <h3 class="text-lg font-bold mb-2">Recent Plots</h3>
        <ul class="space-y-2">
          <%= if Enum.any?(@recent_plots) do %>
            <%= for plot <- @recent_plots do %>
              <li>
                <.link navigate={~p"/plots/#{plot.id}"} class="text-blue-600 hover:underline">
                  Plot #<%= plot.id %> – <%= plot.name %>
                </.link>
              </li>
            <% end %>
          <% else %>
            <li class="text-gray-500">No plots yet. <.link navigate={~p"/plots/new"} class="text-blue-600 hover:underline">Create your first plot</.link></li>
          <% end %>
        </ul>
      </div>

      <div class="bg-white p-6 rounded shadow">
        <h3 class="text-lg font-bold mb-2">Quick Actions</h3>
        <ul class="space-y-2">
          <li><.link href={~p"/plots/new"} class="text-green-600 hover:underline">➕ Create New Plot</.link></li>
        </ul>
      </div>
    </section>
  </main>

  <footer class="mt-12 text-center text-sm text-gray-500">
    &copy; 2025 Plot. Built with Phoenix LiveView.
  </footer>
</body>
</html>